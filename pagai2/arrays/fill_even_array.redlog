load_package redlog;
rlset z;

f :=
   (-i+x+1=0 and -i+n=0 and i-2>=0 and
    -i+x+1=0 and -i+n=0 and cong(i,0,2)) or
   (-i+n+1>=0 and x-1>=0 and i-n>=0 and i-x-3>=0 and
    cong(x+1,0,2) and cong(i,0,2));

g := rlsimpl(cong(x,0,2) and f);

rlqe g;
COMMENT false;